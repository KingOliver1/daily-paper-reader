---
title: "M2H-MX: Multi-Task Dense Visual Perception for Real-Time Monocular Spatial Understanding"
authors: "U. V. B. L. Udugama, George Vosselman, Francesco Nex"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29236v1"
tags: ["query:robot"]
score: 8.0
evidence: 实时多任务视觉感知和空间理解
tldr: 针对单目相机在机器人实时空间感知中的挑战，本文提出M2H-MX多任务感知模型。该模型通过轻量化解码器、全局上下文门控和跨任务交互，实现了高精度的深度与语义预测。其输出可直接集成至单目SLAM系统，显著提升了建图稳定性和轨迹精度，证明了多任务密集预测在实时机器人系统中的可靠性。
selection_source: fresh_fetch
motivation: 单目相机虽成本低，但从单一图像流中实现稳定、实时的空间理解和密集建图仍具有挑战性。
method: 提出一种轻量化多任务模型，引入寄存器门控全局上下文和受控跨任务交互机制，并设计了紧凑的感知到建图接口。
result: "在NYUDv2上达到SOTA性能，并在ScanNet实测中将单目SLAM的平均轨迹误差降低了60.7%。"
conclusion: 现代多任务密集预测技术可以有效地部署于机器人系统，显著增强实时单目空间感知与建图能力。
---

## Abstract
Monocular cameras are attractive for robotic perception due to their low cost and ease of deployment, yet achieving reliable real-time spatial understanding from a single image stream remains challenging. While recent multi-task dense prediction models have improved per-pixel depth and semantic estimation, translating these advances into stable monocular mapping systems is still non-trivial.   This paper presents M2H-MX, a real-time multi-task perception model for monocular spatial understanding. The model preserves multi-scale feature representations while introducing register-gated global context and controlled cross-task interaction in a lightweight decoder, enabling depth and semantic predictions to reinforce each other under strict latency constraints. Its outputs integrate directly into an unmodified monocular SLAM pipeline through a compact perception-to-mapping interface.   We evaluate both dense prediction accuracy and in-the-loop system performance. On NYUDv2, M2H-MX-L achieves state-of-the-art results, improving semantic mIoU by 6.6% and reducing depth RMSE by 9.4% over representative multi-task baselines. When deployed in a real-time monocular mapping system on ScanNet, M2H-MX reduces average trajectory error by 60.7% compared to a strong monocular SLAM baseline while producing cleaner metric-semantic maps. These results demonstrate that modern multi-task dense prediction can be reliably deployed for real-time monocular spatial perception in robotic systems.

---

## 论文详细总结（自动生成）

### M2H-MX: 实时单目空间理解的多任务密集视觉感知论文总结

#### 1. 核心问题与研究背景
*   **研究动机**：单目相机因其低成本和易部署性在机器人领域极具吸引力，但从单一图像流中获取可靠的实时空间理解（深度和语义）仍具挑战。
*   **核心问题**：现有的多任务密集预测模型虽然提升了精度，但在集成到实时单目SLAM（即时定位与建图）系统时面临三大障碍：
    1.  **延迟限制**：预测必须具有低且可预测的延迟，以避免阻塞建图后端。
    2.  **帧间稳定性**：不一致的深度或语义预测会破坏跟踪和地图融合。
    3.  **集成复杂性**：感知模块应能通过固定接口集成，无需修改底层的SLAM算法。
*   **论文目标**：提出 M2H-MX 模型，旨在严格的运行时约束下，通过多任务预测增强单目空间感知能力。

#### 2. 方法论
M2H-MX 采用了一种结合基础模型能力与轻量化解码器的架构：
*   **骨干网络与适配**：使用冻结的 **DINOv3** 作为骨干网络，通过 **LoRA (Low-Rank Adaptation)** 对最后12个 Transformer 块进行参数高效微调。引入层次化特征适配器（HFA）构建多尺度特征金字塔。
*   **寄存器门控曼巴解码器 (RGM)**：
    *   利用 DINOv3 的“寄存器标记（Register Tokens）”提取全局场景上下文。
    *   使用 **Mamba 块** 进行高效的长距离特征传播，并通过寄存器驱动的通道门控机制将全局上下文注入每个解码尺度。
*   **跨任务交互与精炼**：
    *   **跨任务混合器 (CTM)**：允许相关任务（如深度、语义、法线、边缘）之间进行受控的特征交换，利用互补线索。
    *   **多尺度卷积注意力 (MSCA)**：在特征融合后进行空间维度的残差精炼。
*   **任务头**：深度预测采用基于分箱（Bin-based）的预测头，语义预测采用轻量化卷积头。
*   **系统集成**：设计了一个紧凑的感知到建图接口，将预测结果直接输入未修改的 **Mono-Hydra** SLAM 管道。

#### 3. 实验设计
*   **数据集**：
    *   **NYUDv2**：室内场景，评估深度、语义、法线和边缘预测。
    *   **Cityscapes**：室外场景，评估语义和视差预测。
    *   **ScanNet**：用于实时的单目 SLAM 系统级评估。
*   **对比方法**：
    *   **密集预测基准**：TaskPrompter, MTMamba, InvPT, MLoRE, M2H 等。
    *   **SLAM 系统基准**：iMAP, NICE-SLAM, DROID-SLAM, Go-SLAM。
*   **评估指标**：语义 mIoU、深度 RMSE、绝对轨迹误差 (ATE)。

#### 4. 资源与算力
*   **训练细节**：论文未明确说明具体的训练硬件（如 GPU 型号或数量）及总训练时长。
*   **推理性能**：在异步 SLAM 循环中，感知模块在 GPU 上运行，建图后端在 CPU 上运行，系统能维持 **15–20 Hz** 的稳定端到端帧率。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流密集预测基准（NYUDv2, Cityscapes）上进行了性能对比。
    *   在 ScanNet 上进行了系统级的 SLAM 轨迹误差测试。
    *   **消融实验**：针对 CTM、MSCA、RGM 模块以及不同骨干网络（DINOv2, ConvNeXt）进行了详细对比。
*   **充分性评价**：实验设计较为充分，不仅关注算法本身的指标（精度），还重点评估了算法在实际机器人系统（SLAM 闭环）中的表现，具有较强的客观性和说服力。

#### 6. 主要结论与发现
*   **精度提升**：在 NYUDv2 上，M2H-MX-L 相比之前的 SOTA 模型，语义 mIoU 提升了 **6.6%**，深度 RMSE 降低了 **9.4%**。
*   **系统增益**：在 ScanNet 的单目建图测试中，集成 M2H-MX 后，平均轨迹误差 (ATE) 相比强基准（Go-SLAM）降低了 **60.7%**。
*   **稳定性**：证明了高质量的单目多任务预测能显著提升相机跟踪的稳定性和度量语义地图的清晰度。

#### 7. 优点
*   **系统级视角**：不只是刷榜单，而是解决了感知模型如何服务于下游 SLAM 任务的实际痛点。
*   **架构平衡**：巧妙结合了强大的基础模型（DINOv3）和轻量化的 Mamba 解码器，在精度和实时性之间取得了平衡。
*   **即插即用**：感知到建图的接口设计使得该模型可以无缝集成到现有的 SLAM 框架中。

#### 8. 不足与局限
*   **骨干网络依赖**：消融实验显示模型性能高度依赖于 DINOv3 的特征质量，若更换为较弱的骨干网络，性能下降剧烈。
*   **算力细节缺失**：未提供训练所需的具体算力资源消耗，难以评估中小规模团队的复现成本。
*   **任务固定**：虽然支持多任务，但目前主要针对深度和语义，对于更复杂的动态物体跟踪或长期空间推理的支撑尚待验证。

（完）
