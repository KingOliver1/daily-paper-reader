---
title: "Quadruped Parkour Learning: Sparsely Gated Mixture of Experts with Visual Input"
title_zh: 四足机器人跑酷学习：基于视觉输入的稀疏门控专家混合模型
authors: "Michael Ziegltrum, Jianhao Jiao, Tianhu Peng, Chengxu Zhou, Dimitrios Kanoulas"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19344v1"
tags: ["query:robot"]
score: 10.0
evidence: 结合视觉输入与混合专家模型的四足机器人运动
tldr: "本研究针对四足机器人视觉跑酷任务，提出利用稀疏门控专家混合（MoE）架构替代传统的全连接多层感知机（MLP）。在极具挑战性的地形（如高台）中，MoE 架构通过在推理时仅激活部分参数，实现了性能与计算效率的优化平衡。实验证明，在 Unitree Go2 机器人上，MoE 策略在大型障碍物上的成功率比同等推理开销的 MLP 翻了一番，且在达到相同性能时比全量 MLP 节省了 14.3% 的计算时间。"
selection_source: fresh_fetch
motivation: 旨在解决传统 MLP 架构在处理复杂视觉跑酷任务时，难以在有限的计算资源下进一步提升模型容量和控制性能的问题。
method: 引入稀疏门控 MoE 架构，通过门控机制动态选择专家网络，在保持推理速度的同时显著增加模型的总参数量。
result: "实验表明 MoE 策略在复杂障碍物上的成功率是基准 MLP 的两倍，且比具有相同总参数量的 MLP 运行速度快 14.3%。"
conclusion: 稀疏门控 MoE 架构在视觉机器人控制领域展现出优异的扩展性和效率，是提升复杂运动任务性能的有效范式。
---

## 摘要
机器人跑酷为推进在极具挑战性地形（包括高台等大型不连续地形）上的运动能力提供了一个极具吸引力的基准。最近的方法展示了令人印象深刻的能力，包括动态攀爬和跳跃，但通常依赖于具有密集激活层的顺序多层感知器（MLP）架构。相比之下，稀疏门控专家混合（MoE）架构已在大语言模型领域作为一种有效的范式出现，通过在推理时仅激活参数的一个子集来提高可扩展性和性能。在这项工作中，我们研究了稀疏门控 MoE 架构在基于视觉的机器人跑酷中的应用。我们在推理时激活参数数量一致的受控设置下，比较了基于标准 MLP 和 MoE 架构的控制策略。在真实的 Unitree Go2 四足机器人上的实验结果表明了明显的性能提升，与标准 MLP 基准相比，MoE 策略在穿越大型障碍物时的成功尝试次数翻了一番。我们进一步表明，要使标准 MLP 达到相当的性能，需要将其参数数量扩展到与 MoE 模型总量一致，这将导致计算时间增加 14.3%。这些结果强调，稀疏门控 MoE 架构在性能和计算效率之间提供了良好的权衡，从而能够更好地扩展基于视觉的机器人跑酷控制策略。代码库的匿名链接为 https://osf.io/v2kqj/files/github?view_only=7977dee10c0a44769184498eaba72e44。

## Abstract
Robotic parkour provides a compelling benchmark for advancing locomotion over highly challenging terrain, including large discontinuities such as elevated steps. Recent approaches have demonstrated impressive capabilities, including dynamic climbing and jumping, but typically rely on sequential multilayer perceptron (MLP) architectures with densely activated layers. In contrast, sparsely gated mixture-of-experts (MoE) architectures have emerged in the large language model domain as an effective paradigm for improving scalability and performance by activating only a subset of parameters at inference time. In this work, we investigate the application of sparsely gated MoE architectures to vision-based robotic parkour. We compare control policies based on standard MLPs and MoE architectures under a controlled setting where the number of active parameters at inference time is matched. Experimental results on a real Unitree Go2 quadruped robot demonstrate clear performance gains, with the MoE policy achieving double the number of successful trials in traversing large obstacles compared to a standard MLP baseline. We further show that achieving comparable performance with a standard MLP requires scaling its parameter count to match that of the total MoE model, resulting in a 14.3\% increase in computation time. These results highlight that sparsely gated MoE architectures provide a favorable trade-off between performance and computational efficiency, enabling improved scaling of control policies for vision-based robotic parkour. An anonymized link to the codebase is https://osf.io/v2kqj/files/github?view_only=7977dee10c0a44769184498eaba72e44.

---

## 论文详细总结（自动生成）

以下是对论文《Quadruped Parkour Learning: Sparsely Gated Mixture of Experts with Visual Input》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持计算效率的同时，提升四足机器人在复杂视觉跑酷任务（如攀爬、跳跃）中的控制策略性能和可扩展性？
*   **研究动机**：
    *   传统的四足机器人强化学习（RL）通常采用密集激活的多层感知机（MLP），在处理复杂地形时，增加模型容量往往会导致推理延迟增加。
    *   大语言模型（LLM）中流行的**稀疏门控专家混合（MoE）**架构允许模型拥有巨大参数量的同时，在推理时仅激活一小部分参数。
    *   本研究旨在验证 MoE 架构在结合视觉输入的机器人运动控制中，是否能比传统 MLP 提供更好的性能与效率权衡。

### 2. 核心方法论
*   **核心思想**：将 Actor 网络的中层替换为稀疏门控 MoE 层。该层包含多个专家网络（Experts），通过一个门控网络（Gating Network）动态选择最相关的专家参与计算。
*   **关键技术细节**：
    *   **MoE 架构**：采用 16 个专家网络，推理时仅激活前 4 个（Top-4 gating）。通过在损失函数中加入“负载均衡损失”（Load Balancing Loss），防止模型过度依赖少数专家，确保专家多样性。
    *   **两阶段训练（Teacher-Student）**：
        *   **第一阶段**：使用特权信息（如地面摩擦力、精确高度图）训练基于 PPO 的教师策略。
        *   **第二阶段**：通过模仿学习训练学生策略，将特权信息替换为实际可获得的传感器数据（如深度相机图像、本体感受历史）。
    *   **视觉处理流水线**：使用 GRU 编码深度图像，并引入了复杂的 Sim-to-Real 噪声模型（包括裁剪、随机伪影、高斯模糊和轮廓退化），以弥合仿真与现实的差距。
    *   **正则化在线自适应（ROA）**：通过本体感受历史估计环境参数（如质心偏移、电机强度）。

### 3. 实验设计
*   **实验平台**：Unitree Go2 四足机器人，配备 Intel RealSense D435f 深度相机。
*   **实验场景**：
    *   **仿真环境**：Isaac Gym，包含盒子攀爬、间隙跳跃、跨栏等障碍。
    *   **现实环境**：实验室内的 32cm 高箱子（机器人高度的 80%）、室外滑板场及复杂障碍区。
*   **对比基准（Baselines）**：
    *   **不同规模的顺序 MLP**：Small、Medium、Large（推理参数量与 MoE 相当）、Extra-Large（总参数量与 MoE 相当）。
    *   **消融实验**：对比了无噪声训练、无在线自适应（ROA）的情况。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090 GPU** 进行仿真训练。
*   **推理算力**：策略在离站的 **NVIDIA RTX 2060 GPU** 上运行（通过网络与机器人板载电脑通信），控制频率为 50Hz，深度图像发布频率为 15Hz。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到在 3000 步后停止特权信息编码器的变化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对核心障碍物（32cm 箱子）进行了多次重复实验（表现差的策略测试 5 次，表现好的测试 10 次）。
    *   进行了 1000 次前向传播测试以测量平均推理时间。
    *   包含室外泛化性测试和专家权重分析实验。
*   **充分性评价**：实验设计较为客观，通过控制“推理时参数量”和“总参数量”两个维度与 MLP 进行公平对比，充分证明了 MoE 的稀疏优势。消融实验验证了噪声模型和自适应模块的必要性。

### 6. 主要结论与发现
*   **性能翻倍**：在推理开销相同的情况下，MoE 策略在穿越大型障碍物时的成功率是标准 MLP 的两倍。
*   **效率优势**：要达到与 MoE 相当的鲁棒性，MLP 需要扩展到与 MoE 总参数量一致，但这会导致推理计算时间增加 **14.3%**。
*   **专家专业化**：分析发现专家权重呈现明显的**周期性信号**，表明不同的专家在步态周期的不同阶段（如支撑相与摆动相）或不同任务（如视觉处理与本体感受平衡）中实现了自发的分工。

### 7. 优点与亮点
*   **架构创新**：首次将稀疏门控 MoE 应用于带视觉输入的四足机器人跑酷任务，验证了条件计算（Conditional Compute）在具身智能中的潜力。
*   **实战性强**：在真实机器人上实现了高度挑战性的动作（克服 80% 身高的障碍），并提供了详细的深度图像降噪处理方案。
*   **开源贡献**：提供了匿名化的代码库和相机支架设计，便于社区复现。

### 8. 不足与局限
*   **超参数敏感性**：MoE 引入了更多难以调优的超参数（专家数量、Top-k 选择、负载均衡系数等），增加了训练复杂性。
*   **硬件依赖**：虽然推理效率高，但 MoE 的总内存占用较大，对于内存受限的嵌入式设备可能存在部署压力。
*   **场景多样性**：现实实验主要集中在箱子类障碍物，对于更复杂的非几何障碍（如软质地面、移动物体）的覆盖较少。

（完）
