---
title: Iterated Invariant EKF for Quadruped Robot Odometry
title_zh: 用于四足机器人里程计的迭代不变扩展卡尔曼滤波
authors: "Hilton Marques Souza Santana, João Carlos Virgolino Soares, Sven Goffin, Ylenia Nisticò, Silvère Bonnabel, Claudio Semini, Marco Antonio Meggiolaro"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15449v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于四足机器人里程计和状态估计的迭代不变扩展卡尔曼滤波
tldr: 针对四足机器人状态估计中非线性动力学和测量模型带来的挑战，本文提出了一种基于迭代不变扩展卡尔曼滤波（IterIEKF）的开源里程计算法。该方法利用足端接触和基体速度的运动学约束，仅依靠本体感受测量实现估计。实验证明，IterIEKF在精度和一致性上均优于传统的IEKF及基于SO(3)的卡尔曼滤波及其迭代变体，具有更强的环境鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决传统卡尔曼滤波在处理四足机器人非线性动力学和测量模型时存在的精度与一致性不足的问题。
method: 提出一种基于李群理论的迭代不变扩展卡尔曼滤波算法，利用足端运动学约束进行纯本体感受的状态更新。
result: 在仿真和真实数据集上，该算法在估计精度和一致性方面均显著优于Vanilla IEKF及基于SO(3)的滤波算法。
conclusion: IterIEKF为四足机器人提供了一种鲁棒且高效的状态估计方案，验证了迭代更新在非线性李群框架下的优越性。
---

## 摘要
基于卡尔曼滤波的算法是移动机器人的基础，因为它们为具有挑战性的状态估计问题提供了一种计算高效的解决方案。然而，这些算法依赖于两个在实践中难以满足的主要假设：(a) 系统动力学必须是具有高斯过程噪声的线性系统，(b) 观测模型也必须是具有高斯观测噪声的线性模型。以往的工作通过不变扩展卡尔曼滤波 (IEKF) 将假设 (a) 扩展到了非线性空间，表明当系统动力学在李群上满足群仿射性质时，它保留了与经典卡尔曼滤波相似的特性。最近，文献 [1] 针对相同的非线性设置解决了假设 (b) 的对应问题。通过提出的迭代不变扩展卡尔曼滤波 (IterIEKF)，该工作的作者证明了其更新步骤展现出经典线性卡尔曼滤波的若干兼容特性。在这项工作中，我们介绍了一种基于 IterIEKF 的新型开源足式机器人状态估计算法。所提滤波器的更新步骤仅依赖于本体感受测量，利用了足部接触期间的速度运动学约束以及基座坐标系速度，使其对环境条件具有固有的鲁棒性。通过广泛的数值模拟和在真实世界数据集上的评估，我们证明了 IterIEKF 在准确性和一致性方面均优于原始 IEKF、基于 SO(3) 的卡尔曼滤波及其迭代变体。

## Abstract
Kalman filter-based algorithms are fundamental for mobile robots, as they provide a computationally efficient solution to the challenging problem of state estimation. However, they rely on two main assumptions that are difficult to satisfy in practice: (a) the system dynamics must be linear with Gaussian process noise, and (b) the measurement model must also be linear with Gaussian measurement noise. Previous works have extended assumption (a) to nonlinear spaces through the Invariant Extended Kalman Filter (IEKF), showing that it retains properties similar to those of the classical Kalman filter when the system dynamics are group-affine on a Lie group. More recently, the counterpart of assumption (b) for the same nonlinear setting was addressed in [1]. By means of the proposed Iterated Invariant Extended Kalman Filter (IterIEKF), the authors of that work demonstrated that the update step exhibits several compatibility properties of the classical linear Kalman filter. In this work, we introduce a novel open-source state estimation algorithm for legged robots based on the IterIEKF. The update step of the proposed filter relies solely on proprioceptive measurements, exploiting kinematic constraints on foot velocity during contact and base-frame velocity, making it inherently robust to environmental conditions. Through extensive numerical simulations and evaluation on real-world datasets, we demonstrate that the IterIEKF outperforms the vanilla IEKF, the SO(3)-based Kalman Filter, and its iterated variant in terms of both accuracy and consistency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Iterated Invariant EKF for Quadruped Robot Odometry》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**四足机器人在复杂环境下的高精度状态估计（里程计）**。
*   **研究动机**：传统的扩展卡尔曼滤波（EKF）依赖于系统动力学和测量模型的线性化假设。在四足机器人中，由于非线性动力学、频繁的足端接触切换以及传感器噪声，传统 EKF 往往会出现统计不一致性（估计协方差与实际误差不符）和线性化误差过大的问题。
*   **背景**：虽然不变扩展卡尔曼滤波（IEKF）通过李群理论解决了动力学方程的线性化问题，但其更新步骤（测量模型）仍采用单次线性化。本文旨在引入**迭代不变扩展卡尔曼滤波（IterIEKF）**，通过在更新步骤中进行迭代优化，进一步提升估计的精度和一致性。

### 2. 论文提出的方法论
核心思想是将**不变性理论（Invariance）**与**迭代优化（Gauss-Newton 迭代）**相结合。
*   **状态表示**：将机器人的姿态、速度和位置嵌入到 $SE_2(3)$ 矩阵李群中，利用右不变误差（Right-Invariant Error）定义状态偏差。
*   **测量模型**：仅依赖本体感受传感器（IMU 和编码器）。利用足端接触时的运动学约束，将基体速度作为观测值。
*   **迭代更新（核心技术）**：
    1.  将更新步骤重新表述为一个**最大后验概率（MAP）估计问题**。
    2.  在李群切空间内利用 **Gauss-Newton 法**进行多次迭代，重新线性化测量残差。
    3.  通过迭代寻找后验分布的众数（Mode），而非仅仅是单次线性化的均值，从而显著降低强非线性环境下的偏差。
*   **算法流程**：预测步骤遵循 $SE_2(3)$ 的群仿射动力学；更新步骤通过循环计算卡尔曼增益、更新状态误差向量，直至收敛或达到最大迭代次数。

### 3. 实验设计
论文设计了从仿真到现实的梯度实验体系：
*   **实验场景**：
    1.  **MuJoCo 仿真**：进行“8字形”轨迹运动，测试大初始误差下的收敛速度。
    2.  **合成数据 Monte Carlo 模拟**：在理想噪声模型下验证统计一致性。
    3.  **Gazebo 复杂地形仿真**：在非平整、多坡度的地形上评估俯仰角（Pitch）和速度估计。
    4.  **真实世界数据集**：使用 Anymal D 机器人在瑞士 Pilatus 山的徒步数据（GrandTour 数据集），包含长距离、大坡度和楼梯场景。
*   **对比方法（Benchmark）**：
    *   **IEKF**：标准的不变扩展卡尔曼滤波。
    *   **$SO(3)$-EKF**：传统的基于旋转矩阵/四元数的分离式 EKF。
    *   **Iter $SO(3)$-EKF**：传统 EKF 的迭代变体。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 或 CPU 型号及训练时长。
*   **算法特性**：由于该方法属于递归滤波算法（Recursive Filtering），其计算开销主要集中在矩阵求逆和多次迭代（通常 3-6 次）。作者强调该算法具有极高的计算效率，能够满足四足机器人实时控制的需求（实验中传感器频率高达 400Hz）。
*   **开源资源**：作者提供了开源代码库，包含仿真工具以促进复现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 100 次和 50 次重复的 **Monte Carlo 模拟**，用于评估 NEES（标准估计误差平方）指标，这在统计学上是充分且客观的。
    *   真实数据集测试涵盖了约 250 米长、持续 400 秒的复杂户外路径。
*   **充分性评价**：实验设计非常充分。作者不仅对比了精度（RMSE/MAE），还深入分析了**统计一致性**（Consistency）和**收敛速度**。通过引入不同等级的初始误差和测量噪声，客观地展示了迭代更新在不同条件下的增益。

### 6. 主要结论与发现
*   **精度提升**：在仿真中，IterIEKF 相比标准 IEKF 将基体速度和重力方向估计误差降低了约 **60%**；在真实数据中，俯仰角误差降低了约 **12%**。
*   **收敛速度**：在大初始误差情况下，IterIEKF 达到相同精度所需的收敛时间比 IEKF 缩短了 **40% - 60%**。
*   **一致性**：IterIEKF 的 NEES 指标最接近理论值，证明其对自身估计不确定性的表达比其他三种滤波器更准确，有效避免了过度自信（Overconfidence）问题。
*   **观测性**：验证了在仅有速度反馈的情况下，位置和偏航角（Yaw）是不可观测的，但 IterIEKF 能更好地维持可观测子空间（速度、横滚、俯仰）的准确性。

### 7. 优点
*   **理论与工程结合**：首次将 IterIEKF 应用于四足机器人领域，并给出了完整的几何推导。
*   **鲁棒性**：纯本体感受方案不依赖视觉或激光雷达，使其在光照变化、烟雾或无特征环境中依然可靠。
*   **几何一致性**：利用李群结构保证了状态流形的约束，避免了欧几里得空间线性化导致的结构性偏差。
*   **开源贡献**：提供了完整的框架和仿真环境，对社区贡献较大。

### 8. 不足与局限
*   **协方差依赖性**：迭代滤波对测量协方差（Measurement Covariance）的建模非常敏感。如果噪声模型（如足端滑移）建模不准，迭代可能会放大错误。
*   **噪声水平限制**：实验发现，当测量噪声极大（如严重打滑）时，迭代带来的精度增益会显著减小，甚至与普通 IEKF 持平。
*   **未考虑偏置估计**：本文简化了模型，未在状态向量中加入 IMU 的零偏（Bias）估计，这在长时间导航中可能会导致漂移加剧。
*   **非高斯噪声处理**：虽然迭代减少了线性化误差，但对于足端接触产生的非高斯、脉冲式噪声，仍缺乏专门的鲁棒核函数处理。

（完）
