---
title: Natural Gradient Gaussian Approximation Filter on Lie Groups for Robot State Estimation
title_zh: 李群上用于机器人状态估计的自然梯度高斯近似滤波器
authors: "Tianyi Zhang, Wenhan Cao, Chang Liu, Yao Lyu, Shengbo Eben Li"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10057v1"
tags: ["query:robot"]
score: 9.0
evidence: 基于李群的足式机器人状态估计
tldr: 针对足式机器人等在李群流形上的状态估计问题，本文提出了一种名为 NANO-L 的新型滤波器。该方法将滤波问题重构为高斯增量变量的参数优化问题，通过自然梯度法和费舍尔信息矩阵处理流形曲率，从而避免了传统滤波器的局部线性化误差。实验证明，该算法在保持高效计算的同时，显著提升了复杂地形下的估计精度。
selection_source: fresh_fetch
motivation: 传统李群滤波器依赖切空间的局部线性化来处理非线性观测，这在处理弯曲流形时会产生显著的累积误差。
method: 提出 NANO-L 滤波器，将滤波转化为增量优化问题，并利用自然梯度法和李导数扰动模型推导出闭式协方差更新公式。
result: "在 Unitree GO2 机器人上的硬件实验表明，NANO-L 的估计误差比主流滤波器降低了约 40%，且计算开销相近。"
conclusion: NANO-L 通过结合自然梯度优化与李群几何特性，有效解决了非线性观测下的状态估计难题，具有极高的实用价值。
---

## 摘要
对于在李群流形上演化的机器人系统（如足式机器人）而言，准确的状态估计是实现敏捷控制的前提。然而，定义在弯曲流形上的非线性观测模型给这一任务带来了挑战。现有滤波器依赖于切空间中的局部线性化来处理此类非线性，从而导致了累积估计误差。为解决这一局限性，我们将流形滤波重新表述为一个关于高斯分布增量变量的参数优化问题，从而避免了线性化。在该表述下，增量可以通过指数算子映射到李群，并以乘法形式作用于先验估计，从而得到后验状态。我们进一步提出了一种用于解决该问题的自然梯度优化方案，其迭代过程利用增量变量的 Fisher 信息矩阵来考虑切空间的曲率。这产生了一种名为“李群自然梯度高斯近似（NANO-L）”滤波器的迭代算法。利用李导数中的扰动模型，我们证明了对于机器人定位任务中广泛采用的不变观测模型，NANO-L 中的协方差更新具有精确的闭式解，消除了迭代更新的需求，从而提高了计算效率。在不同地形上运行的宇树 GO2 足式机器人硬件实验表明，在计算成本相当的情况下，NANO-L 的估计误差比常用滤波器降低了约 40%。

## Abstract
Accurate state estimation for robotic systems evolving on Lie group manifolds, such as legged robots, is a prerequisite for achieving agile control. However, this task is challenged by nonlinear observation models defined on curved manifolds, where existing filters rely on local linearization in the tangent space to handle such nonlinearity, leading to accumulated estimation errors. To address this limitation, we reformulate manifold filtering as a parameter optimization problem over a Gaussian-distributed increment variable, thereby avoiding linearization. Under this formulation, the increment can be mapped to the Lie group through the exponential operator, where it acts multiplicatively on the prior estimate to yield the posterior state. We further propose a natural gradient optimization scheme for solving this problem, whose iteration process leverages the Fisher information matrix of the increment variable to account for the curvature of the tangent space. This results in an iterative algorithm named the Natural Gradient Gaussian Approximation on Lie Groups (NANO-L) filter. Leveraging the perturbation model in Lie derivative, we prove that for the invariant observation model widely adopted in robotic localization tasks, the covariance update in NANO-L admits an exact closed-form solution, eliminating the need for iterative updates thus improving computational efficiency. Hardware experiments on a Unitree GO2 legged robot operating across different terrains demonstrate that NANO-L achieves approximately 40% lower estimation error than commonly used filters at a comparable computational cost.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NANO-L**（Natural Gradient Gaussian Approximation on Lie Groups）的新型滤波器，旨在解决机器人（特别是足式机器人）在李群流形上进行状态估计时的非线性处理难题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：足式机器人等系统的状态（姿态、位置、速度）演化在非欧几里得的**李群（Lie Group）**流形上。
*   **核心问题**：传统的流形滤波器（如 EKF、UKF-M、InEKF）通常依赖于在切空间（Tangent Space）进行**局部线性化**（泰勒展开或无迹变换）。在处理强非线性观测或高曲率流形时，这种线性化会引入显著的近似误差，导致估计精度下降甚至发散。
*   **研究动机**：通过信息几何和优化视角重新审视滤波问题，消除对局部线性化的依赖，提升机器人在复杂地形下的估计鲁棒性。

### 2. 方法论
*   **核心思想**：将流形上的状态估计问题重构为一个**高斯变分推断（Variational Inference）优化问题**。它不直接更新状态，而是优化切空间中一个高斯分布的“增量变量”的参数（均值和协方差）。
*   **关键技术细节**：
    *   **参数优化表述**：将后验分布的寻找转化为最小化 KL 散度的过程，目标函数包含期望对数似然项和正则化项（惩罚均值、协方差及体积的偏差）。
    *   **自然梯度下降（NGD）**：利用统计流形的 **Fisher 信息矩阵（FIM）** 来指导优化方向。相比普通梯度下降，NGD 能更好地处理切空间的几何曲率，使参数收敛更稳定。
    *   **闭式协方差更新**：论文证明了对于机器人定位中常用的“不变观测模型”（如腿式里程计），其似然函数的 Hessian 矩阵是解析可求且与增量无关的。这使得协方差更新具有**精确的闭式解**，无需迭代，极大地降低了计算开销。
    *   **算法流程**：
        1.  **预测**：利用 IMU 动力学在李群上进行状态传播。
        2.  **增量先验计算**：在切空间传播增量的均值和协方差。
        3.  **自然梯度优化**：迭代更新增量均值（协方差一步到位），直至收敛。
        4.  **状态更新**：将优化后的增量通过指数映射作用于先验状态。

### 3. 实验设计
*   **实验场景**：
    1.  **数值仿真**：视觉辅助惯性导航系统，包含 3 个静态地标，进行 100 次蒙特卡洛实验。
    2.  **硬件实验**：使用 **Unitree GO2** 四足机器人，在**平坦地面**和**不稳定地形**（如松软/易滑表面）上进行测试。
*   **Benchmark（对比方法）**：
    *   欧几里得滤波器：EKF, UKF, NANO。
    *   流形滤波器：**InEKF**（不变 EKF，目前足式机器人的 SOTA 方法）, **UKF-M**（流形无迹卡尔曼滤波）。
*   **评估指标**：均方根误差（RMSE）、绝对轨迹误差（ATE）、相对误差（RE）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Core i9-14900HX** 处理器的笔记本电脑上运行。
*   **计算效率**：NANO-L 的单次更新耗时约为 **3.35 ms**。
*   **实时性**：该耗时远低于 200Hz 采样频率要求的 5ms 预算，证明了算法在嵌入式或移动平台上的实时可行性。

### 5. 实验数量与充分性
*   **仿真部分**：进行了 **100 次蒙特卡洛试验**，提供了统计意义上的误差分布（箱线图），结果具有说服力。
*   **硬件部分**：在两种代表性地形下各采集了 **5 组 60 秒**的数据集，并使用了高精度 NOKOV 动捕系统作为真值。
*   **消融实验**：针对迭代次数对精度和耗时的影响做了专门分析，验证了“单次迭代即可达到极高精度”的实用性结论。
*   **评价**：实验设计客观、公平，对比了从经典到前沿的多种方法，覆盖了从理论验证到实际应用的完整链路。

### 6. 主要结论与发现
*   **精度大幅提升**：在不稳定地形下，NANO-L 的位置 ATE 比 InEKF 降低了约 **41%**。
*   **几何感知的优势**：实验证明，尊重李群几何结构并使用自然梯度优化，能有效抑制由于地形不稳定导致的观测噪声和非线性偏差。
*   **效率与精度的平衡**：得益于闭式协方差解，NANO-L 在精度远超 EKF 的同时，计算成本保持在同一数量级。

### 7. 优点
*   **理论创新**：首次将自然梯度高斯近似引入李群滤波，为非线性流形估计提供了新的数学框架。
*   **数学优雅性**：利用李导数扰动模型推导出闭式解，巧妙地解决了迭代优化可能带来的计算负担。
*   **实用性强**：在真实四足机器人上验证了其在复杂、非结构化地形下的鲁棒性。

### 8. 不足与局限
*   **模型依赖性**：闭式协方差更新高度依赖于“不变观测模型”，如果传感器模型不满足此特性（例如某些复杂的视觉特征关联），计算效率可能会下降。
*   **超参数调节**：算法引入了收敛阈值 $\gamma$ 等超参数，在不同传感器配置下可能需要微调。
*   **垂直漂移**：虽然精度提升显著，但在长距离实验中，受限于腿式里程计的固有观测性问题，高度方向（Z轴）仍存在一定漂移。

（完）
