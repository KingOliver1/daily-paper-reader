---
title: Simulator Adaptation for Sim-to-Real Learning of Legged Locomotion via Proprioceptive Distribution Matching
title_zh: 基于本体感受分布匹配的足式运动仿真到现实学习的仿真器自适应
authors: "Jeremy Dao, Alan Fern"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11090v1"
tags: ["query:robot"]
score: 9.0
evidence: 通过本体感受分布匹配进行足式机器人运动的从仿真到现实学习
tldr: 针对足式机器人从仿真到现实迁移中的动力学差异问题，本文提出了一种基于本体感受分布匹配的仿真器自适应方法。该方法通过比较硬件和仿真中关节观测与动作的分布，无需时间对齐或外部传感器即可优化仿真参数。在Go2机器人上的实验表明，该方法仅需不到5分钟的硬件数据即可显著减少运动漂移，在双足行走等挑战性任务中表现优异，为仿真器校准提供了一种实用且高效的方案。
selection_source: fresh_fetch
motivation: 传统的仿真器校准方法依赖于高精度的时间对齐和外部动捕系统，在实际应用中受限且操作复杂。
method: 提出一种将硬件与仿真数据视为本体感受观测和动作分布进行匹配的黑盒优化方法，用于调整仿真器的动力学参数和执行器模型。
result: 在Go2四足机器人的仿真和实物实验中，该方法在无需特权信息的情况下达到了与基准方法相当的参数恢复精度，并显著提升了策略的现实表现。
conclusion: 本体感受分布匹配为足式机器人提供了一种无需外部感知、高效且实用的仿真器自适应途径，有效解决了Sim-to-Real中的动力学失配问题。
---

## 摘要
在仿真中训练的足式运动策略由于仿真器与现实世界之间的动力学差异，在硬件上往往表现出性能下降，这凸显了对仿真器本身进行自适应以更好地匹配硬件行为的方法的需求。先前的工作通常通过对关节和基座轨迹进行精确的时间对齐匹配来量化这些差异。这一过程需要运动捕捉、特权传感以及精心控制的初始条件。我们提出了一种基于本体感受分布匹配的实用替代方案，该方案将硬件和仿真轨迹（rollouts）作为关节观测和动作的分布进行比较，从而消除了对时间对齐或外部传感的需求。利用该指标作为黑盒目标，我们探索了通过参数辨识、动作增量模型（action-delta models）和残差执行器模型来调整仿真器动力学。在 Go2 四足机器人上的广泛仿真到仿真（sim-to-sim）消融实验中，我们的方法在参数恢复和策略性能提升方面达到了与特权状态匹配基准相当的效果。现实世界实验表明，即使对于具有挑战性的双腿行走行为，使用不到五分钟的硬件数据也能显著减少漂移。这些结果表明，本体感受分布匹配为学习型足式运动的仿真到现实迁移提供了一条实用且有效的仿真器自适应路径。

## Abstract
Simulation trained legged locomotion policies often exhibit performance loss on hardware due to dynamics discrepancies between the simulator and the real world, highlighting the need for approaches that adapt the simulator itself to better match hardware behavior. Prior work typically quantify these discrepancies through precise, time-aligned matching of joint and base trajectories. This process requires motion capture, privileged sensing, and carefully controlled initial conditions. We introduce a practical alternative based on proprioceptive distribution matching, which compares hardware and simulation rollouts as distributions of joint observations and actions, eliminating the need for time alignment or external sensing. Using this metric as a black-box objective, we explore adapting simulator dynamics through parameter identification, action-delta models, and residual actuator models. Our approach matches the parameter recovery and policy-performance gains of privileged state-matching baselines across extensive sim-to-sim ablations on the Go2 quadruped. Real-world experiments demonstrate substantial drift reduction using less than five minutes of hardware data, even for a challenging two-legged walking behavior. These results demonstrate that proprioceptive distribution matching provides a practical and effective route to simulator adaptation for sim-to-real transfer of learned legged locomotion.

---

## 论文详细总结（自动生成）

这篇论文针对足式机器人从仿真到现实（Sim-to-Real）迁移中的动力学失配问题，提出了一种无需外部感知设备的仿真器自适应方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：仿真训练的策略在真实机器人上往往因动力学差异（如摩擦、延迟、执行器非线性）而性能下降。
*   **研究背景**：
    *   **动力学随机化（DR）**：虽能提高鲁棒性，但往往导致行为过于保守，且无法真正消除仿真与现实的差距。
    *   **传统系统辨识（System ID）**：依赖于时间对齐的轨迹匹配，通常需要昂贵的运动捕捉（Mocap）系统来获取基座状态，且对初始条件极其敏感，在复杂的足式运动中难以实施。
*   **整体含义**：本文旨在开发一种**仅利用机器人自身本体感受数据（Proprioception）**、无需时间对齐、无需外部传感器的实用仿真器校准方案。

### 2. 方法论
*   **核心思想**：将仿真和现实的轨迹视为**状态-动作分布**而非时间序列。通过匹配这两个分布的统计特性来调整仿真器参数。
*   **关键技术细节**：
    *   **分布度量（Wasserstein Cost）**：使用 Wasserstein 距离衡量仿真数据 $D_{sim}$ 与硬件数据 $D_{hw}$ 的差异。为解决高维计算开销，采用了 **1D 边缘分布近似**（计算每个关节维度上的 1D Wasserstein 距离并取平均），兼顾了计算效率和样本效率。
    *   **特征选择**：匹配关节位置、关节速度和策略输出动作的分布。
    *   **仿真器修改模型**：探索了三种层级的自适应方式：
        1.  **静态参数（FricArm）**：调整关节摩擦和电枢（Armature）参数。
        2.  **动作增量模型（Action-Delta）**：在策略输出上叠加一个微型神经网络预测的修正值。
        3.  **残差执行器模型（ResidAct）**：在 PD 控制层级叠加一个 1kHz 的残差转矩修正。
    *   **优化算法**：使用黑盒优化算法 **CMA-ES** 最小化分布差异代价函数，从而搜索最优的仿真器修改参数。

### 3. 实验设计
*   **实验平台**：Unitree Go2 四足机器人。
*   **实验场景**：
    1.  **模型参数偏移（Sim-to-Sim）**：验证能否从带噪声的数据中恢复已知的摩擦和电枢参数。
    2.  **弹簧关节（Sim-to-Sim & Sim-to-Real）**：在后腿关节添加物理弹簧，模拟复杂的非线性动力学改变。
    3.  **双足行走（Sim-to-Real）**：让四足机器人仅靠后腿行走，这是一个极具挑战性且存在巨大仿真鸿沟的任务。
*   **对比基准（Benchmark）**：
    *   **MatchS**：假设拥有特权信息（基座位置/速度）的时间对齐状态匹配。
    *   **MatchO**：仅利用观测值但进行时间对齐匹配。
    *   **Wass（本文方法）**：基于分布匹配，无需特权信息和时间对齐。

### 4. 资源与算力
*   **仿真环境**：使用 NVIDIA IsaacLab 进行大规模并行仿真。
*   **训练算法**：PPO（近端策略优化）。
*   **数据规模**：策略训练使用了 4096 个并行环境，总计约 19.66 亿个样本。
*   **硬件数据**：仅需**不到 5 分钟**的真实硬件运行数据即可完成自适应。
*   **算力说明**：文中未明确标注具体的 GPU 型号和具体训练时长，但提到使用了大规模并行强化学习框架，通常需要高性能 NVIDIA GPU（如 A100 或 RTX 3090/4090 级别）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从理想环境下的参数恢复到带噪声环境的鲁棒性测试，再到复杂的实物迁移。
*   **消融实验**：对比了不同的代价函数（MatchS, MatchO, Wass）和不同的仿真器修改模型（FricArm, ActionDelta, ResidAct）。
*   **充分性评价**：实验设计较为充分。通过 Sim-to-Sim 建立了受控的量化评估，通过 Sim-to-Real 验证了实际应用价值。样本量（10,000 条评估轨迹）足以保证统计意义上的客观性。

### 6. 主要结论与发现
*   **分布匹配的有效性**：在无需特权基座信息的情况下，基于分布匹配的方法在参数辨识精度上达到了与“拥有上帝视角”的 MatchS 方法相当的水平。
*   **模型表达力**：对于简单的参数偏差，静态参数调整即可；但对于复杂的动力学改变（如弹簧关节），**残差执行器模型（ResidAct）** 表现最优。
*   **现实表现**：在 Go2 机器人的双足行走实验中，该方法显著减少了运动漂移（部分方向漂移减少达 50% 以上）。

### 7. 优点与亮点
*   **实用性极强**：摆脱了对运动捕捉系统和精密实验环境的依赖，仅需关节编码器数据。
*   **计算高效**：1D 边缘分布近似解决了 Wasserstein 距离在高维空间下的计算难题。
*   **鲁棒性高**：相比于时间对齐方法，分布匹配对传感器噪声和时间延迟不敏感。

### 8. 不足与局限
*   **初始策略依赖**：如果初始策略在现实中表现极差（例如机器人直接摔倒），则无法收集到有效的分布数据进行匹配。
*   **失效模式处理**：在 Digit 人形机器人上的尝试表明，该方法难以处理“彻底失败”（如向后摔倒）的情况，因为失败过程具有高度随机性和非确定性，分布匹配难以捕捉其因果结构。
*   **黑盒优化限制**：CMA-ES 在处理极高维度的神经网络参数时可能面临收敛困难。

（完）
