---
title: Cross-Modal Reinforcement Learning for Navigation with Degraded Depth Measurements
title_zh: 针对深度测量退化情况下的跨模态强化学习导航
authors: "Omkar Sawant, Luca Zanatta, Grzegorz Malczyk, Kostas Alexis"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22182v1"
tags: ["query:robot"]
score: 8.0
evidence: 退化环境下的跨模态强化学习导航
tldr: 针对深度传感器在恶劣光照或反射表面下失效的问题，本文提出一种跨模态强化学习框架。通过跨模态 Wasserstein 自动编码器（CMWAE）学习深度与灰度图像的共享潜空间表示，使系统能在深度测量退化时利用灰度图推断深度特征。结合强化学习策略，实现了在非结构化环境中的鲁棒避障导航，并在实机实验中验证了其跨场景迁移能力。
selection_source: fresh_fetch
motivation: 旨在解决深度传感器在复杂环境（如强光、反射）中因测量失效而导致机器人导航性能下降的问题。
method: 提出一种跨模态 Wasserstein 自动编码器来学习深度与灰度图的共享表示，并将其集成到强化学习策略中。
result: 实验证明该方法在深度测量严重受损时仍能保持鲁棒的避障性能，并具备良好的实机迁移能力。
conclusion: 通过跨模态一致性学习共享特征，可以有效提升机器人在传感器退化环境下的自主导航可靠性。
---

## 摘要
本文提出了一种跨模态学习框架，利用深度图像和灰度图像的互补信息来实现鲁棒导航。我们引入了一种跨模态 Wasserstein 自动编码器（Cross-Modal Wasserstein Autoencoder），通过强制跨模态一致性来学习共享的潜在表示，使系统能够在深度测量受损时从灰度观测中推断出与深度相关的特征。学习到的表示与基于强化学习的策略相结合，用于在光照不足或反射表面等不利条件下导致深度传感器退化的非结构化环境中实现无碰撞导航。仿真和真实世界实验表明，我们的方法在深度显著退化的情况下仍能保持鲁棒性能，并成功迁移到真实环境中。

## Abstract
This paper presents a cross-modal learning framework that exploits complementary information from depth and grayscale images for robust navigation. We introduce a Cross-Modal Wasserstein Autoencoder that learns shared latent representations by enforcing cross-modal consistency, enabling the system to infer depth-relevant features from grayscale observations when depth measurements are corrupted. The learned representations are integrated with a Reinforcement Learning-based policy for collision-free navigation in unstructured environments when depth sensors experience degradation due to adverse conditions such as poor lighting or reflective surfaces. Simulation and real-world experiments demonstrate that our approach maintains robust performance under significant depth degradation and successfully transfers to real environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cross-Modal Reinforcement Learning for Navigation with Degraded Depth Measurements》的结构化技术总结：

### 1. 核心问题与研究动机
*   **研究背景**：在非结构化环境（如地下隧道、灾区）中，无人机导航高度依赖深度传感器。然而，深度传感器在强光、反射表面或低纹理区域极易失效（产生空洞或噪声）。
*   **核心问题**：传统的模块化导航或单模态端到端学习在深度图退化时表现脆弱。
*   **研究动机**：利用灰度图像与深度图像之间的互补性。即使深度图损坏，灰度图通常仍能提供环境结构线索。通过跨模态学习，使机器人能在深度测量不可靠时，利用视觉特征“补全”导航所需的几何信息。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出一种**跨模态 Wasserstein 自动编码器 (CMWAE)**，将灰度图和深度图映射到一个共享的低维潜空间（Latent Space），并在此基础上训练强化学习（RL）导航策略。
*   **关键技术细节**：
    *   **CMWAE 架构**：采用双分支编码器（基于 AlexNet），分别处理灰度图和深度图。通过一个共享的投影网络生成融合潜向量 $z$。
    *   **损失函数**：结合了**重构损失**（仅重构深度图，迫使模型从灰度图中提取几何特征）和基于 **MMD（最大均值差异）** 的正则化项。使用 Wasserstein 距离而非传统 VAE 的 KL 散度，以获得更稳定、质量更高的潜在表示。
    *   **强化学习策略**：使用 **PPO (Proximal Policy Optimization)** 算法。观测值包括潜向量 $z$、目标相对位置、机器人状态（速度、姿态）及历史动作。
    *   **训练策略**：在训练 CMWAE 时引入两种损坏方案（S1, S2），通过随机遮盖深度图的 25%-50% 区域，增强模型在缺失数据下的推理能力。

### 3. 实验设计
*   **数据集**：使用 **TartanAir** 室内轨迹数据集进行 CMWAE 的预训练（提供同步的 RGB-D 图像）。
*   **仿真环境**：在 **Aerial Gym** 模拟器中进行 RL 训练，采用课程学习（Curriculum Learning），障碍物密度随训练进度增加。
*   **Benchmark（对比方法）**：对比了 **DCE-RL**（深度碰撞编码强化学习），这是一种仅依赖深度信息的单模态先进方法。
*   **测试场景**：
    *   **Clean（清洁）**：无损坏的深度输入。
    *   **Corrupted（损坏）**：深度图带有随机遮盖块，模拟传感器失效。
    *   **实机实验**：在 28 米长的走廊中布置障碍物，通过关闭 Realsense D455 的红外投影仪人为制造深度退化。

### 4. 资源与算力
*   **硬件平台**：实机部署使用了 **NVIDIA Jetson Orin NX (16GB)** 嵌入式计算平台。
*   **仿真平台**：使用了支持高度并行化的 Aerial Gym 模拟器。
*   **说明**：文中未明确给出具体的训练时长或所使用的离线训练 GPU 型号（如 A100 或 RTX 4090），但提到使用了并行化仿真来加速 RL 训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真**：在 20 个难度等级（课程等级）下进行了数千次测试。
    *   **实机**：在 7 种不同的障碍物布局环境下进行了飞行实验。
*   **充分性评价**：实验设计较为充分。不仅对比了重构精度（MSE/SSIM），还对比了闭环导航的成功率、碰撞率和超时率。通过消融实验验证了不同损坏方案对模型鲁棒性的影响。

### 6. 主要结论与发现
*   **鲁棒性提升**：在深度图损坏 50% 的极端情况下，传统 DCE-RL 成功率为 0%，而 CMWAE-RL 仍能保持约 32.8% 的成功率（在简单场景下更高）。
*   **性能权衡**：在“清洁”数据下，CMWAE 的表现略逊于单模态方法（因为共享潜空间分散了表征能力），但在“退化”数据下具有压倒性优势。
*   **Sim2Real 迁移**：实验证明在仿真中学习到的跨模态特征能成功迁移至真实世界，有效处理了真实环境中的传感器噪声和光照变化。

### 7. 优点与亮点
*   **跨模态一致性**：通过 Wasserstein 距离约束潜空间，比传统的 VAE 导航框架更具泛化性。
*   **实用性强**：针对真实世界中常见的传感器失效问题（如玻璃反射、强光直射）提供了切实可行的端到端解决方案。
*   **高效部署**：模型架构轻量（AlexNet 骨干），适合在无人机等资源受限的嵌入式平台上实时运行。

### 8. 不足与局限
*   **潜空间容量限制**：由于强制灰度图和深度图共享相同的潜向量维度（64维），在传感器完全正常时，其导航精度不如专门针对深度图优化的单模态模型。
*   **模态单一**：目前仅融合了灰度图和深度图，未引入事件相机（Event Camera）或热成像等在极端环境下更具优势的模态。
*   **环境复杂度**：实验环境主要集中在室内走廊和几何障碍物，对于更复杂的室外动态环境（如移动物体、植被）的验证尚不充分。

（完）
