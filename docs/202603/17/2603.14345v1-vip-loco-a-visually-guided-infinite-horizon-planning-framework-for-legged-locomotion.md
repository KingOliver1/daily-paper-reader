---
title: "VIP-Loco: A Visually Guided Infinite Horizon Planning Framework for Legged Locomotion"
title_zh: VIP-Loco：一种用于足式机器人运动的视觉引导无限时界规划框架
authors: "Aditya Shirwatkar, Satyam Gupta, Shishir Kolathaya"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14345v1"
tags: ["query:robot"]
score: 10.0
evidence: 足式机器人运动的视觉引导无限时界规划
tldr: VIP-Loco是一个结合视觉感知、强化学习与无限时界规划的足式机器人运动框架。针对MPC处理高维感知难和RL缺乏规划能力的问题，该框架在训练阶段利用内部模型提取运动动力学特征，在部署阶段将学习到的模型融入MPC公式中。实验证明该方法在多种机器人形态（四足、双足、轮足）和复杂地形（楼梯、跳跃等）上实现了鲁棒且可解释的运动控制。
selection_source: fresh_fetch
motivation: 旨在解决模型预测控制（MPC）难以处理高维视觉输入以及强化学习（RL）缺乏结构化规划能力的问题。
method: 提出VIP-Loco框架，通过内部模型将本体感受和深度图像映射为特征，并在部署时将其集成到无限时界MPC中。
result: 在四足、双足及轮足机器人上成功实现了斜坡、楼梯、跳跃和攀爬等多种复杂地形的鲁棒运动。
conclusion: VIP-Loco成功统一了规划与感知，为不同形态的足式机器人提供了具备适应性和可解释性的运动控制方案。
---

## 摘要
足式机器人的感知运动需要预判并适应复杂、动态的环境。模型预测控制 (MPC) 作为一个强大的基准，提供了具有约束执行的可解释运动规划，但在处理高维感知输入和快速变化的地形时表现不佳。相比之下，无模型强化学习 (RL) 在视觉挑战场景中具有良好的适应性，但缺乏规划能力。为了弥补这一差距，我们提出了 VIP-Loco，这是一个将基于视觉的场景理解与强化学习及规划相结合的框架。在训练期间，内部模型将本体感受状态和深度图像映射为强化学习策略所使用的紧凑动力学特征。在部署时，学习到的模型被用于无限时界 MPC 公式中，将适应性与结构化规划相结合。我们在具有挑战性的运动任务（包括斜坡、楼梯、爬行、倾斜、跳跃间隙和攀爬）中，针对三种机器人形态（四足机器人 Unitree Go1、双足机器人 Cassie 和轮足机器人 TronA1-W）对 VIP-Loco 进行了仿真验证。通过消融实验以及与最先进方法的对比，我们证明了 VIP-Loco 统一了规划与感知，在多样化环境中实现了鲁棒且可解释的运动。

## Abstract
Perceptive locomotion for legged robots requires anticipating and adapting to complex, dynamic environments. Model Predictive Control (MPC) serves as a strong baseline, providing interpretable motion planning with constraint enforcement, but struggles with high-dimensional perceptual inputs and rapidly changing terrain. In contrast, model-free Reinforcement Learning (RL) adapts well across visually challenging scenarios but lacks planning. To bridge this gap, we propose VIP-Loco, a framework that integrates vision-based scene understanding with RL and planning. During training, an internal model maps proprioceptive states and depth images into compact kinodynamic features used by the RL policy. At deployment, the learned models are used within an infinite-horizon MPC formulation, combining adaptability with structured planning. We validate VIP-Loco in simulation on challenging locomotion tasks, including slopes, stairs, crawling, tilting, gap jumping, and climbing, across three robot morphologies: a quadruped (Unitree Go1), a biped (Cassie), and a wheeled-biped (TronA1-W). Through ablations and comparisons with state-of-the-art methods, we show that VIP-Loco unifies planning and perception, enabling robust, interpretable locomotion in diverse environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《VIP-Loco: A Visually Guided Infinite Horizon Planning Framework for Legged Locomotion》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在足式机器人运动中，将**基于视觉的感知能力**与**长时界的结构化规划**有效结合。
*   **研究动机**：
    *   **MPC（模型预测控制）**：具有可解释性和约束处理能力，但在处理高维视觉输入和快速变化地形时计算压力大、泛化性弱。
    *   **RL（强化学习）**：在视觉挑战场景下适应性强，但通常是反应式的（Reactive），缺乏对未来的显式规划，难以处理需要预判的任务（如跳跃间隙、狭窄通道）。
    *   **目标**：建立一个统一框架，既能利用视觉进行地形预判，又能通过规划确保运动的稳定性和约束满足。

### 2. 方法论
VIP-Loco 框架分为**训练**和**部署**两个阶段：
*   **核心思想**：学习一个“内部模型”（Internal Model），它能将本体感受和深度图像映射为紧凑的动力学特征，并预测未来状态。
*   **关键技术细节**：
    *   **内部模型（RSSM 架构）**：包含 GRU 单元（处理时序记忆）、CNN-MLP 编码器（处理深度图）、随机潜变量（建模不确定性）以及显式的质心（CoM）动力学状态预测。
    *   **想象增强（Imagination Augmentation）**：在训练时，策略不仅看当前观测，还利用内部模型生成的“想象”序列（未来 H 步的动力学状态），使策略具备预判能力。
    *   **变分目标函数**：通过重建深度图和本体观测来学习表征，相比一致性损失（Consistency Loss），它在处理侧向几何约束（如爬行、倾斜）时更有效。
    *   **无限时界 MPC（部署阶段）**：使用学习到的内部模型作为动力学引擎，通过 **MPPI（模型预测路径积分）** 算法进行在线轨迹优化。在规划时界末端引入**价值函数引导（Value Function Bootstrapping）**，从而近似无限时界的累积奖励。

### 3. 实验设计
*   **实验场景**：在 Isaac Gym 仿真环境中设置了 6 种挑战性地形：斜坡（Slopes）、楼梯（Stairs）、攀爬（Climb）、跳跃间隙（Gaps）、低矮爬行（Crawl）、侧身倾斜（Tilt）。
*   **机器人形态**：验证了三种截然不同的形态：
    1.  **Unitree Go1**（四足机器人）
    2.  **Cassie**（双足机器人）
    3.  **TronA1-W**（轮足机器人）
*   **对比基准（Benchmarks）**：
    *   **HIM-Loco**：仅本体感受的 RL。
    *   **PIP-Loco**：本体感受的 RL + MPC 框架。
    *   **WMP**：基于世界模型的视觉 RL（无显式规划）。
    *   **VIP-Loco (Consistency)**：使用一致性损失的变体。

### 4. 资源与算力
*   **硬件**：使用了两块 **NVIDIA RTX A6000 GPU** 和 Intel Xeon Gold 5318Y CPU。
*   **软件/库**：Isaac Gym（物理仿真）、NVIDIA WARP（加速渲染）、JAX（用于部署阶段的高效并行规划）。
*   **训练规模**：支持 4096 个并行环境，显存占用控制在 25GB 以下。
*   **部署性能**：在桌面 GPU 上实现了 **40–50 Hz** 的推理频率，总规划延迟约为 20–25 ms。

### 5. 实验数量与充分性
*   **实验规模**：针对每种机器人形态独立训练，每个实验运行 **5 个随机种子**。
*   **消融实验**：详细对比了“有无规划”、“变分损失 vs 一致性损失”对性能的影响。
*   **充分性评价**：实验覆盖了从简单到极难（Level 0-8）的地形梯度，且跨越了四足、双足、轮足三种主流形态，证明了框架的通用性。对比方法涵盖了纯 RL、视觉 RL 和 RL+MPC 组合，对比逻辑清晰、客观。

### 6. 主要结论与发现
*   **视觉+规划的必要性**：在视觉要求高的任务（跳跃、爬行）中，仅靠本体感受的方法在难度 4 以上基本失效；而 VIP-Loco 在难度 8 仍能保持高成功率。
*   **变分损失的优势**：在需要侧向空间感知的任务（如 Crawl 和 Tilt）中，变分重建损失比一致性损失表现更好，因为后者容易忽略特权信息（Scan dots）覆盖不到的几何细节。
*   **规划的增益**：显式 MPC 规划不仅提高了平均奖励，还增强了运动的可解释性（通过预测的 CoM 轨迹可见）。

### 7. 优点
*   **多形态通用性**：一个框架通用于四足、双足和轮足机器人，无需修改核心算法。
*   **长效预判**：通过无限时界 MPC 解决了传统 MPC 时界短和纯 RL 缺乏规划的问题。
*   **可解释性**：内部模型预测的动力学状态（如质心高度）与实际执行高度吻合，使黑盒 RL 具备了透明的规划逻辑。

### 8. 不足与局限
*   **Sim-to-Real 缺位**：论文所有结果均基于仿真，虽然提到其前作 PIP-Loco 已在实物验证，但 VIP-Loco 尚未展示真实的硬件部署效果。
*   **算力依赖**：部署阶段仍需桌面级 GPU 支持 JAX 加速，对于算力受限的嵌入式车载计算平台（如 Jetson Orin）可能存在实时性挑战。
*   **地形依赖**：虽然地形多样，但仍属于结构化或半结构化地形，对于完全随机、动态变化的极度复杂环境（如茂密丛林）的泛化能力尚待验证。

（完）
