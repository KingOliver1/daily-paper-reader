---
title: Embedding Classical Balance Control Principles in Reinforcement Learning for Humanoid Recovery
title_zh: 将经典平衡控制原理嵌入强化学习以实现人形机器人恢复
authors: "Nehar Poddar, Stephen McCrory, Luigi Penco, Geoffrey Clark, Hakki Erhan Svil, Robert Griffin"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08619v1"
tags: ["query:robot"]
score: 10.0
evidence: 利用经典平衡指标进行人形机器人恢复的强化学习策略
tldr: "针对人形机器人易摔倒且难以自主恢复的问题，本文提出一种将经典平衡控制原理（如捕获点、质心状态和质心动量）嵌入强化学习框架的统一策略。该方法通过在训练中引入特权信息引导奖励函数，使机器人能够实现从微小扰动下的踝/髋策略到剧烈碰撞后的多接触点起身的平滑过渡。在Unitree H1-2机器人上的实验证明，该策略在随机初始姿态下的恢复成功率达93.4%，显著提升了机器人的自主恢复能力。"
selection_source: fresh_fetch
motivation: 现有强化学习恢复策略缺乏对平衡状态的显式表示，导致机器人在非结构化环境中难以应对复杂跌倒和恢复任务。
method: 在强化学习的评论家网络中嵌入捕获点、质心状态等经典平衡指标作为特权输入，并据此设计奖励函数，而执行器仅依赖本体感受。
result: "在Unitree H1-2上实现了93.4%的恢复成功率，涵盖了从平衡调节到多接触点起身的完整恢复频谱，且消融实验证明平衡指标对学习至关重要。"
conclusion: 将可解释的平衡结构嵌入学习框架能显著减少机器人处于失效状态的时间，并扩大其自主恢复的运行包络线。
---

## 摘要
人形机器人仍然容易跌倒并陷入不可恢复的故障状态，这限制了它们在非结构化环境中的实际应用。虽然强化学习已经展示了起身行为，但现有方法将恢复视为纯粹的任务奖励问题，缺乏对平衡状态的显式表示。我们提出了一种统一的强化学习（RL）策略，通过将经典平衡指标（落足点、质心状态和质心动量）嵌入为特权评论家（privileged critic）输入，并在训练期间直接围绕这些量构建奖励函数，从而解决了这一局限性；同时，执行器（actor）仅依赖本体感受，以实现零样本硬件迁移。在没有参考轨迹或预设接触的情况下，单一策略即可涵盖完整的恢复频谱：针对小扰动的踝关节和髋关节策略、大推力下的纠正性步进，以及利用手、肘和膝盖进行多点接触起身的顺从跌倒。该策略在 Isaac Lab 中基于 Unitree H1-2 进行训练，在随机初始姿态和非预设跌倒配置下实现了 93.4% 的恢复率。消融实验表明，移除平衡感知结构会导致起身学习完全失败，这证实了这些指标提供了有意义的学习信号，而非偶然的结构。到 MuJoCo 的仿真到仿真（sim-to-sim）迁移以及初步的硬件实验进一步证明了跨环境的泛化能力。这些结果表明，将可解释的平衡结构嵌入学习框架可以显著减少处于故障状态的时间，并扩大自主恢复的范围。

## Abstract
Humanoid robots remain vulnerable to falls and unrecoverable failure states, limiting their practical utility in unstructured environments. While reinforcement learning has demonstrated stand-up behaviors, existing approaches treat recovery as a pure task-reward problem without an explicit representation of the balance state. We present a unified RL policy that addresses this limitation by embedding classical balance metrics: capture point, center-of-mass state, and centroidal momentum, as privileged critic inputs and shaping rewards directly around these quantities during training, while the actor relies solely on proprioception for zero-shot hardware transfer. Without reference trajectories or scripted contacts, a single policy spans the full recovery spectrum: ankle and hip strategies for small disturbances, corrective stepping under large pushes, and compliant falling with multi-contact stand-up using the hands, elbows, and knees. Trained on the Unitree H1-2 in Isaac Lab, the policy achieves a 93.4% recovery rate across randomized initial poses and unscripted fall configurations. An ablation study shows that removing the balance-informed structure causes stand-up learning to fail entirely, confirming that these metrics provide a meaningful learning signal rather than incidental structure. Sim-to-sim transfer to MuJoCo and preliminary hardware experiments further demonstrate cross-environment generalization. These results show that embedding interpretable balance structure into the learning framework substantially reduces time spent in failure states and broadens the envelope of autonomous recovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《Embedding Classical Balance Control Principles in Reinforcement Learning for Humanoid Recovery》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：人形机器人在非结构化环境中极易跌倒，且现有的自主恢复方法（如起身动作）通常被视为纯粹的任务奖励问题，缺乏对平衡状态的显式物理表示。
*   **研究动机**：
    *   现有的强化学习（RL）恢复策略往往依赖隐式信号，导致评论家（Critic）难以准确评估状态的可恢复性。
    *   传统控制理论（如捕获点 CP）在处理复杂、多接触的恢复任务时过于简化。
    *   目标是开发一种统一的策略，既能处理微小扰动（踝/髋策略），也能处理剧烈跌倒后的多接触点起身。

### 2. 方法论
核心思想是将**经典平衡控制指标**作为“特权信息”嵌入到强化学习框架中。
*   **非对称执行器-评论家（Asymmetric Actor-Critic）架构**：
    *   **执行器（Actor）**：仅接收硬件可测的本体感受信号（角速度、重力投影、关节位置/速度），确保零样本（Zero-shot）迁移。
    *   **评论家（Critic）**：在训练时接收特权信息，包括质心（CoM）位置/速度/加速度、全身动量、以及**捕获点（Capture Point, CP）**位置。
*   **物理引导的奖励设计（三阶段）**：
    1.  **垂直恢复（Group I）**：通过高斯奖励引导质心高度，并对上升/下降速度进行非对称塑造。
    2.  **平衡与可捕获性（Group II）**：利用线性倒立摆（LIP）模型计算 CP，惩罚 CP 超出支撑多边形的配置，并正则化质心动量。
    3.  **正则化与动作塑造（Group III）**：包括关节扭矩限制、姿态对齐、多接触（手/肘）诱导奖励。
*   **训练课程（Curriculum）**：从放宽扭矩限制的探索阶段，逐步过渡到增加外部扰动和随机初始姿态，最后退火至硬件安全限制。

### 3. 实验设计
*   **实验环境**：主要在 **Isaac Lab** 中进行训练（基于 Unitree H1-2 机器人模型）。
*   **验证场景**：
    *   **扰动测试**：施加 50N 到 300N 不等的推力，观察踝关节、髋关节及步进策略的切换。
    *   **多样化姿态恢复**：从俯卧、仰卧、蹲下、跪下等随机姿态起身。
    *   **Sim-to-Sim 迁移**：将 Isaac Lab 训练的策略直接部署到 **MuJoCo** 环境中，测试其在不同物理引擎下的鲁棒性。
    *   **硬件部署**：在真实的 **Unitree H1-2** 机器人上进行 10 次不同姿态的起身实验。
*   **对比基准**：主要通过**消融实验**对比，移除特权信息和平衡奖励项，观察学习效果。

### 4. 资源与算力
*   **软件框架**：Isaac Lab (基于 NVIDIA Isaac Gym)。
*   **算力说明**：文中提到使用了 GPU 加速仿真环境进行大规模并行训练（50,000 次迭代，每次更新包含 24 个 rollout 步长）。
*   **具体型号**：文中**未明确说明**具体的 GPU 型号（如 A100 或 RTX 4090）及具体的训练总时长。

### 5. 实验数量与充分性
*   **仿真规模**：进行了 **10,000 次** 随机初始配置下的恢复实验，成功率达 93.4%，样本量充足。
*   **消融实验**：对比了“完整策略”与“无平衡指标/无特权信息/无课程学习”的简化版本。结果显示简化版在起身任务上的失败率为 100%（始终处于 Stuck-low 状态），有力证明了该方法的必要性。
*   **硬件验证**：进行了 10 次物理实验，涵盖多种姿态，虽然样本量相对仿真较小，但展示了 100% 的成功率和零样本迁移能力。
*   **客观性**：通过跨引擎（MuJoCo）验证和不同高度/方向的推力敏感性分析，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **平衡指标是关键**：显式嵌入 CP 和 CoM 指标不仅是辅助，更是学习起身行为的**必要条件**。
*   **统一策略**：单一神经网络即可覆盖从精细平衡调节到粗犷多点接触起身的完整频谱，无需预设接触序列或参考轨迹。
*   **鲁棒性**：策略对外部扰动表现出阶梯式反应（踝->髋->步进->跌倒支撑），符合人类生物力学特征。

### 7. 优点
*   **无需参考运动**：不依赖动捕数据或预设的关键帧，动作完全由物理目标驱动生成。
*   **可解释性强**：将传统控制理论的 CP 概念引入 RL，使黑盒模型的训练过程有了明确的物理导向。
*   **零样本迁移**：通过非对称架构和大量的领域随机化（Domain Randomization），成功解决了仿真与现实的鸿沟。

### 8. 不足与局限
*   **平面假设**：CP 指标的计算基于平面支撑假设，在极度崎岖或非平面地形上的适用性有待验证。
*   **硬件振荡**：在物理实验中观察到恢复后的瞬时高频关节振荡，反映出仿真与现实在阻尼特性上仍存在微小失配。
*   **计算开销**：虽然执行器很轻量，但训练时需要计算复杂的质心动力学和 CP 指标，增加了训练端的计算负担。
*   **环境感知缺失**：目前主要依赖本体感受，未集成视觉感知，无法在复杂障碍物环境中选择最佳接触点。

（完）
