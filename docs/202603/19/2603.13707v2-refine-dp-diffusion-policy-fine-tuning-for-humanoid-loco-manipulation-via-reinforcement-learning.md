---
title: "REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning"
title_zh: REFINE-DP：通过强化学习进行类人机器人移动操作的扩散策略微调
authors: "Zhaoyuan Gu, Yipu Chen, Zimeng Chai, Alfred Cueva, Thong Nguyen, Yifan Wu, Huishu Xue, Minji Kim, Isaac Legene, Fukang Liu, Matthew Kim, Ayan Barula, Yongxin Chen, Ye Zhao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13707v2"
tags: ["query:robot"]
score: 10.0
evidence: 通过强化学习微调实现人形机器人运动规划与控制
tldr: 针对人形机器人移动操作中高层规划与底层控制解耦导致的分布偏移和任务失败问题，本文提出REFINE-DP框架。该框架通过强化学习（PPO）微调扩散策略（DP）作为高层规划器，并同步优化底层控制器以精准跟踪动态指令。实验证明，该方法在门禁通行和长程搬运任务中表现优异，显著提升了模拟和现实环境中的任务成功率及鲁棒性，解决了高质量演示数据稀缺的挑战。
selection_source: fresh_fetch
motivation: 离线训练的扩散策略与底层控制器之间存在解耦导致的分布偏移，且人形机器人高质量演示数据获取成本极高。
method: 提出一种分层框架，利用基于PPO的扩散策略梯度微调高层规划器，并同步更新底层控制器以适应演进的指令分布。
result: "在模拟环境中实现了超过90%的任务成功率，并成功处理了分布外场景，同时在真实动态环境中实现了平滑的自主任务执行。"
conclusion: 强化学习微调是实现可靠的人形机器人移动操作的关键，能有效解决扩散策略在复杂动态系统中的部署难题。
---

## 摘要
类人机器人的移动操作（loco-manipulation）需要在复杂的机器人-环境动力学和长时程任务下，将协调的高层运动规划与稳定的底层全身执行相结合。虽然扩散策略（DPs）在模仿学习中展现出潜力，但在类人机器人上部署它们面临着严峻挑战：离线训练的运动规划器与底层控制器是解耦的，导致指令跟踪效果差、分布偏移累积以及任务失败。对于高维类人系统而言，通过扩展演示数据规模的常用方法成本极高。为了应对这一挑战，我们提出了 REFINE-DP（扩散策略的强化学习微调），这是一个联合优化 DP 高层规划器和基于强化学习（RL）的底层移动操作控制器的分层框架。DP 通过基于 PPO 的扩散策略梯度进行微调以提高任务成功率，同时控制器同步更新以精确跟踪规划器不断演变的指令分布，从而减少降低运动质量的分布失配。我们在执行移动操作任务（包括穿过门和长时程物体搬运）的类人机器人上验证了 REFINE-DP。REFINE-DP 在仿真中实现了超过 90% 的成功率，即使是在预训练数据中未见的分布外情况下也是如此，并能在现实世界的动态环境中实现平滑的自主任务执行。我们提出的方法显著优于预训练的 DP 基准，并证明了 RL 微调是实现可靠类人机器人移动操作的关键。

## Abstract
Humanoid loco-manipulation requires coordinated high-level motion plans with stable, low-level whole-body execution under complex robot-environment dynamics and long-horizon tasks. While diffusion policies (DPs) show promise for learning from demonstrations, deploying them on humanoids poses critical challenges: the motion planner trained offline is decoupled from the low-level controller, leading to poor command tracking, compounding distribution shift, and task failures. The common approach of scaling demonstration data is prohibitively expensive for high-dimensional humanoid systems. To address this challenge, we present REFINE-DP (REinforcement learning FINE-tuning of Diffusion Policy), a hierarchical framework that jointly optimizes a DP high-level planner and an RL-based low-level loco-manipulation controller. The DP is fine-tuned via a PPO-based diffusion policy gradient to improve task success rate, while the controller is simultaneously updated to accurately track the planner's evolving command distribution, reducing the distributional mismatch that degrades motion quality. We validate REFINE-DP on a humanoid robot performing loco-manipulation tasks, including door traversal and long-horizon object transport. REFINE-DP achieves an over $90\%$ success rate in simulation, even in out-of-distribution cases not seen in the pre-trained data, and enables smooth autonomous task execution in real-world dynamic environments. Our proposed method substantially outperforms pre-trained DP baselines and demonstrates that RL fine-tuning is key to reliable humanoid loco-manipulation. https://refine-dp.github.io/REFINE-DP/

---

## 论文详细总结（自动生成）

### 论文总结：REFINE-DP —— 通过强化学习微调实现类人机器人移动操作

#### 1. 核心问题与整体含义（研究动机和背景）
人形机器人的**移动操作（Loco-manipulation）**要求高层运动规划与底层全身控制高度协同。目前主流的**扩散策略（Diffusion Policy, DP）**虽然在模仿学习中表现优异，但在人形机器人上部署时面临三大痛点：
*   **解耦导致的分布偏移：** 离线训练的规划器与底层控制器之间存在鸿沟，执行过程中的微小误差会随时间累积，导致指令跟踪失败。
*   **数据获取成本极高：** 对于高维的人形机器人系统，通过大规模采集人类演示数据来覆盖所有可能状态是不现实的。
*   **动态环境适应性差：** 纯模仿学习难以处理预训练数据之外（OOD）的动态干扰。

本文提出的 **REFINE-DP** 旨在通过强化学习（RL）微调，将高层规划与底层控制进行“联合优化”，从而在少量演示数据的基础上实现高成功率和高鲁棒性的自主操作。

#### 2. 方法论：核心思想与关键技术
REFINE-DP 采用分层控制架构，分为三个阶段：

*   **核心思想：** 联合优化高层 DP 规划器和底层 RL 控制器。不仅让规划器学会“怎么走”，还让控制器学会“如何精准跟踪规划器产生的特定动态指令”。
*   **关键技术细节：**
    1.  **分层架构：** 高层 DP 输出低维任务指令（基座速度、手部位姿）；底层 RL 控制器（解耦为上肢和下肢策略）负责全身平衡与指令跟踪。
    2.  **DPPO 微调：** 采用扩散策略策略优化（DPPO）算法，将 DP 的去噪过程建模为一个多步决策的 MDP（马尔可夫决策过程），利用 PPO 算法通过环境反馈的稀疏奖励直接更新 DP 参数。
    3.  **联合优化（Joint Optimization）：** 这是本文的核心创新。在微调阶段，不仅更新 DP，还同步更新底层 RL 控制器。这解决了指令分布不匹配的问题，使控制器能适应 DP 产生的连续、动态的轨迹指令，而非预训练时的静态指令。
*   **算法流程：** 采集少量演示数据 $\rightarrow$ 预训练 DP $\rightarrow$ 在仿真环境（IsaacLab）中进行联合 RL 微调 $\rightarrow$ 部署至真实机器人。

#### 3. 实验设计
*   **实验场景：**
    1.  物体拾取（Object Pickup）。
    2.  长程搬运（Pick-and-place）。
    3.  开门并穿行（Door traversal）。
    4.  台阶辅助物体检索（Stair-assisted retrieval）。
*   **Benchmark 与对比方法：**
    *   **DiT/MLP Planner：** 纯预训练的 Transformer 或 MLP 架构扩散策略。
    *   **MLP-FT：** 对 MLP 架构进行 RL 微调。
    *   **Residual RL：** 在冻结的 DP 之上学习残差补偿策略。
    *   **DiT-FT (Ours)：** 仅微调 DP。
    *   **Joint Optimized (Ours)：** 联合微调 DP 和底层控制器。

#### 4. 资源与算力
*   **硬件平台：** 使用了 **Booster T1** 类人机器人（29 自由度）。
*   **计算设备：** 策略计算机配备 **AMD 7945HX CPU** 和 **NVIDIA RTX 4060 GPU**。
*   **运行频率：** DP 规划器运行在 10Hz（使用 TensorRT 加速），底层 RL 控制器运行在 50Hz。
*   **训练环境：** 使用 NVIDIA **IsaacLab** 进行 GPU 加速的并行仿真训练。
*   **未明确说明：** 文中未给出具体的总训练时长（小时/天）或使用的并行环境数量。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 4 个复杂任务进行了评估，每个任务包含 100 次随机试验。
*   **消融实验：** 涵盖了不同网络骨干（Transformer vs MLP）、不同微调策略（残差 vs 直接微调）、以及有无联合优化的对比。
*   **数据效率实验：** 专门对比了“增加演示数据量”与“进行 RL 微调”的效果，证明了微调在小数据集（50 条轨迹）下的优越性。
*   **充分性评价：** 实验设计较为全面，不仅关注成功率，还量化了跟踪误差、运动平滑度和任务完成时间。引入了 OOD（分布外）测试和真实世界部署，验证了方法的实用性。

#### 6. 主要结论与发现
*   **成功率大幅提升：** REFINE-DP 在仿真中将任务成功率从预训练的 50-70% 提升至 **90% 以上**。
*   **联合优化的必要性：** 联合优化不仅提高了成功率，还显著降低了手部跟踪误差（位置误差降低，姿态误差降低达 50%），并使运动更平滑。
*   **数据效率：** 使用 50 条轨迹进行微调的效果，优于使用 1000 条轨迹进行纯离线训练的效果。
*   **鲁棒性：** 微调后的策略展现出“重试”能力（如开门没抓准时会自动调整步态重新尝试），并能适应比预训练范围大数倍的初始位置随机化。

#### 7. 优点
*   **解决解耦痛点：** 通过联合优化打破了高层规划与底层执行之间的壁垒。
*   **高效微调：** 证明了在人形机器人领域，RL 微调比单纯堆砌演示数据更具成本效益。
*   **自主纠错：** 赋予了机器人动态调整和失败重试的能力，增强了在真实动态环境中的生存力。

#### 8. 不足与局限
*   **感知依赖：** 实验高度依赖 **MoCap（运动捕捉系统）** 获取物体状态，尚未实现完全基于车载视觉（如 RGB-D）的闭环控制。
*   **真实世界性能下降：** 仿真成功率 >90%，但实测成功率在 50%-70% 之间，说明仍存在显著的 **Sim-to-Real 鸿沟**（主要源于传感器噪声和地面摩擦力建模不准）。
*   **任务复杂度限制：** 虽然涵盖了开门和搬运，但尚未涉及更精细的双手协作或极高动态的交互任务。

（完）
